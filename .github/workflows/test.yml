name: Test Workflow

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-slim
    steps:
      - name: Run tests
        run: |
          echo "Running tests..."
          echo "Tests completed successfully!"
